// Global shortcuts are now registered from the frontend via
// @tauri-apps/plugin-global-shortcut JS API, which correctly handles
// CommandOrControl mapping (Cmd on macOS, Ctrl on Windows/Linux)
// and allows dynamic re-registration when the user changes settings.
