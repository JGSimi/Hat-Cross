# hat-core: lógica de domínio pura. REGRA: zero dependências de tauri/objc2/
# windows-sys — este crate roda `cargo test` em qualquer runner de CI.
[package]
name = "hat-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
